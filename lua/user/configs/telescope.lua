local builtin = require('telescope.builtin')
local themes = require('telescope.themes')

require('telescope').setup{
  defaults = {
    -- ...
  },
  pickers = {
    find_files = {
    }
  },
  extensions = {
    -- ...
  }
}

